package pageObjects;

import org.openqa.selenium.WebDriver;

public class AssetTokenGidView {
    WebDriver driver;
    public AssetTokenGidView(WebDriver driver){
        this.driver = driver;
    }
}
