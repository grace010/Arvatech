package actions.pageObjects;

import org.openqa.selenium.WebDriver;

import actions.commons.*;

public class AssetTokenDetail extends BasePage {
    WebDriver driver;
    public AssetTokenDetail(WebDriver driver){
        this.driver = driver;
    }
}
