package actions.pageObjects;

import org.openqa.selenium.WebDriver;

import actions.commons.*;

public class AssetTokenGidView extends BasePage{
    WebDriver driver;
    public AssetTokenGidView(WebDriver driver){
        this.driver = driver;
    }
}
